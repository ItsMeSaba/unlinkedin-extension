{
  "name": "linkedout",
  "version": "1.0.0",
  "description": "LinkedIn feed filter extension",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "fs-extra": "^11.2.0"
  },
  "packageManager": "yarn@4.9.1"
}
